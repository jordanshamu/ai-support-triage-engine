# Core
pandas>=1.5
numpy>=1.23
scikit-learn>=1.1

# Visualisation
matplotlib>=3.6

# Notebook
jupyter>=1.0
nbformat>=5.0

# LLM engine (LIVE mode only — optional; offline mode needs no key)
anthropic>=0.39
